<ul >
  {{#events}}
    <li>
        <h2>{{ title }}</h2>
        <p>{{ description }}</p>
        <p>at {{ location }}</p>
        <p>likes: {{ likes }} <form action="/event/{{id}}/like" method="post">       
        <input type="submit" name="submit" value="like" class="btn btn-primary"/>
        </form></p>
    </li>
  {{/events}}
</ul>